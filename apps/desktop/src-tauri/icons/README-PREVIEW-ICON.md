# Temporary Monument app icon

The Intel alpha preview uses the exact user-provided four-disc purple/blue artwork as its temporary application icon.

The release branch installs the supplied source asset into `src-tauri/icons/icon.png` before Tauri generates the full macOS icon set. This is temporary branding and is intentionally isolated from product/source-editing behavior.
