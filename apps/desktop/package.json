{
  "name": "monument-desktop-prototype",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 4173 -d .",
    "check": "find src native -type f | grep -E '\\.(js|mjs)$' | xargs -n1 node --check",
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build_static.mjs"
  }
}
