# Visual Editor M2.3 — Tailwind + JSX/TSX

This filename is retained as a compatibility pointer for earlier branch references.

The **single canonical M2.3 implementation and trust-boundary spec** is:

[`VISUAL_EDITOR_M2_MARKUP_OWNERSHIP.md`](VISUAL_EDITOR_M2_MARKUP_OWNERSHIP.md)

Do not maintain a second copy of the M2.3 contract here. Product/architecture changes belong in the canonical markup-ownership spec and `MASTER_PRODUCT_CONTEXT.md` in the same PR.
