# EPIC

Multi-area or multi-session work whose full implementation cannot stay safely inside one focused context.

`research -> architecture/spec -> dependency graph -> slice plans -> fresh context packets -> isolated execution -> integration -> full verification -> fresh review -> handoff/learning`

The orchestrator owns contracts and integration. Workers receive bounded packets, not the whole conversation.
