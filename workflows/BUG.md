# BUG

Observed incorrect behavior where cause is not already mechanical.

`inspect -> reproduce -> localize/minimize -> falsifiable hypotheses -> root-cause fix -> regression evidence -> fresh review`

Do not stack speculative changes. If user impact is live/critical, route to `INCIDENT`.
