# SHIP

Integration/release/deploy readiness.

`re-run applicable gates -> independent review -> check rollback/observability -> ensure no unresolved blockers -> prepare PR/release/deploy handoff`

Production writes remain human-gated unless an approved pipeline contract explicitly authorizes them.
