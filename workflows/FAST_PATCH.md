# FAST_PATCH

Use only for clear, low-risk changes with obvious acceptance and no public contract, data/migration, auth/security, production, or irreversible implications.

`inspect-codebase -> implement-slice -> verify-change -> diff review`

Required evidence is targeted and proportional. If risk or ambiguity grows, re-route instead of forcing the patch through this lane.
