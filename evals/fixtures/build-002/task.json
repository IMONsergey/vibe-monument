{
  "id": "build-002",
  "class": "BUILD",
  "request": "Implement parse_headers: case-insensitive normalized keys, trim whitespace, ignore malformed lines, last duplicate wins.",
  "starting_commit": "fixture-generated-at-run",
  "acceptance": [
    "hidden_test.py exits 0"
  ],
  "permissions": "workspace-only; no network required",
  "hidden_test": "hidden_test.py"
}
