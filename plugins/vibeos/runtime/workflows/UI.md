# UI

User-facing visual or interaction work.

`inspect product/design system -> clarify visual/behavior contract -> plan -> implement -> live visual QA -> accessibility review -> runtime verification -> fresh code review`

When a screenshot/design is authoritative, add `ui-reference-fidelity`. When loading/rendering performance could regress, add `performance-check`.
