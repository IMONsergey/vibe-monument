# BUILD

Normal bounded feature/refactor work.

`inspect -> clarify/spec -> plan slices -> implement -> deterministic verification -> fresh code review -> learn if reusable`

Add TDD when behavior is testable before implementation. Add security, visual, performance, or adversarial review when their triggers apply. Use an evidence ledger for material work.
