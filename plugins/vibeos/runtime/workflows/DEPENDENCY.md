# DEPENDENCY

Framework/library/runtime/SDK/tool upgrades.

`inspect current usage -> primary migration/release docs -> compatibility plan -> version/lockfile change -> integration fixes -> targeted/full verification -> fresh review`

Do not attribute unrelated lockfile churn to the requested upgrade.
